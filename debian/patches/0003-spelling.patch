From: "Barak A. Pearlmutter" <barak+git@pearlmutter.net>
Date: Mon, 28 Dec 2020 22:05:18 +0000
Subject: spelling

---
 Constants.h       | 2 +-
 Interpreter.cpp   | 6 +++---
 Interpreter.h     | 2 +-
 MainWindow.cpp    | 2 +-
 RunController.cpp | 4 ++--
 5 files changed, 8 insertions(+), 8 deletions(-)

diff --git a/Constants.h b/Constants.h
index 314e100..980f512 100644
--- a/Constants.h
+++ b/Constants.h
@@ -50,7 +50,7 @@
 	#define RUNSTATESTOP 0
 	#define RUNSTATERUN 1
 	#define RUNSTATEDEBUG 2
-	#define RUNSTATESTOPING 3
+	#define RUNSTATESTOPPING 3
 	#define RUNSTATERUNDEBUG 4
 
 	// imagesave valid image types
diff --git a/Interpreter.cpp b/Interpreter.cpp
index ece5f25..602f590 100644
--- a/Interpreter.cpp
+++ b/Interpreter.cpp
@@ -575,8 +575,8 @@ bool Interpreter::isStopped() {
 
 bool Interpreter::isStopping() {
 	// interpreter is stopped or is about to stop
-	// to avoid RunController::stopRun() to be triggered while status == R_STOPING too
-	return (status == R_STOPPED || status == R_STOPING);
+	// to avoid RunController::stopRun() to be triggered while status == R_STOPPING too
+	return (status == R_STOPPED || status == R_STOPPING);
 }
 
 void Interpreter::setStatus(run_status s) {
@@ -1057,7 +1057,7 @@ Interpreter::run() {
 	sound->error = &error;
 	srand(time(NULL)+QTime::currentTime().msec()*911L); rand(); rand(); 	// initialize the random number generator for this thread
 	runtimer.start(); // used by MSEC function
-	while (status != R_STOPING && execByteCode() >= 0) {} //continue
+	while (status != R_STOPPING && execByteCode() >= 0) {} //continue
 	debugMode = 0;
 	cleanup(); // cleanup the variables, databases, files, stack and everything
 	emit(stopRunFinalized(!isError));
diff --git a/Interpreter.h b/Interpreter.h
index dbba1d2..2ab238d 100644
--- a/Interpreter.h
+++ b/Interpreter.h
@@ -56,7 +56,7 @@
     #include <QSerialPort>
 #endif
 
-enum run_status {R_STOPPED, R_RUNNING, R_INPUT, R_STOPING};
+enum run_status {R_STOPPED, R_RUNNING, R_INPUT, R_STOPPING};
 
 #define NUMFILES 8
 #define NUMSOCKETS 8
diff --git a/MainWindow.cpp b/MainWindow.cpp
index 306c3a8..bf49447 100644
--- a/MainWindow.cpp
+++ b/MainWindow.cpp
@@ -776,7 +776,7 @@ void MainWindow::setRunState(int state) {
     debugact->setEnabled(userCanInteractWithGUI && isEditorWindowActive);
     stepact->setEnabled(state==RUNSTATEDEBUG || state==RUNSTATERUNDEBUG);
     bpact->setEnabled(state==RUNSTATEDEBUG);
-    stopact->setEnabled(state!=RUNSTATESTOP && state!=RUNSTATESTOPING);
+    stopact->setEnabled(state!=RUNSTATESTOP && state!=RUNSTATESTOPPING);
     clearbreakpointsact->setEnabled(state!=RUNSTATERUN && isEditorWindowActive);
 
     // Clear command for toolbars
diff --git a/RunController.cpp b/RunController.cpp
index e3f4812..9522bd0 100644
--- a/RunController.cpp
+++ b/RunController.cpp
@@ -404,10 +404,10 @@ void RunController::stopRun() {
 	if(!i->isStopping()){
 	// event when the user clicks on the stop button
 	mainwin->statusBar()->showMessage(tr("Stopping."));
-	mainwin->setRunState(RUNSTATESTOPING);
+	mainwin->setRunState(RUNSTATESTOPPING);
 	bool stopinput = i->isAwaitingInput();
 
-	i->setStatus(R_STOPING);//no more ops
+	i->setStatus(R_STOPPING);//no more ops
 
 	mymutex->lock();
 	// stop input only when input is waitted
